[project]
name = "qwen38-27b-ollama"
version = "0.1.0"
description = "Download Qwen3.8-27B GGUFs, merge split shards, and import into Ollama"
requires-python = ">=3.10"
dependencies = [
    "huggingface_hub[hf_xet]>=0.34",
]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
package = false
