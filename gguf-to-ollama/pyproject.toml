[project]
name = "gguf-to-ollama"
version = "0.1.0"
description = "Download GGUFs from Hugging Face, merge split shards, and import into Ollama"
requires-python = ">=3.10"
dependencies = [
    "huggingface_hub[hf_xet]>=0.34",
]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
package = false
